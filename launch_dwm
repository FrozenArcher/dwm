#!/bin/sh

if [[ -z "$DWM_DIR" ]]; then
    export DWM_DIR="$HOME/dwm"
fi

export STATUSBAR="dwmblocks"
export DWM_BAR="$DWM_DIR/scripts/bar"

dwmblocks &

while true; do
	# Log stderror to a file
	dwm 2> $HOME/.dwm.log
done

killall dwmblocks
